How to run the script:
Please run static mode directly, because running default mode will take a long time.
Because the house price data of the webpage is updated in real time, the results calculated by running the static mode and the default mode may be different.
The scatter plot needs to be run in jupyter notebook to display it, just import scatterplot.ipynb directly into jupyter notebook and run it.
